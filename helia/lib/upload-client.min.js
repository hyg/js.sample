// 简化版 @web3-storage/upload-client
(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
    typeof define === 'function' && define.amd ? define(['exports'], factory) :
    (factory((global['@web3-storage/upload-client'] = {})));
  }(this, (function (exports) { 'use strict';
  
    class UploadApi {
      async createToken(did, name = 'Token') {
        // 模拟与 web3.storage 服务的交互
        // 在真实场景中，这会发送一个 POST 请求
        const token = `SWT.${btoa(did.id + name + Date.now()).replace(/\//g, '_').replace(/\+/g, '-')}`;
        return token;
      }
    }
  
    exports.UploadApi = UploadApi;
  
  })));